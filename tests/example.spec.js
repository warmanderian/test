// @ts-check
const { test, expect } = require('@playwright/test');

/*test('has footer', async ({ page }) => {
  await page.goto('https://creditural.ru/');

  await page.locator(".footer__see.hidden-mobile").click()
  await page.waitForTimeout(10000);
});
*/
